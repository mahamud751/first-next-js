


const Blogs = ({ data }) => {
    return (
        <div>
            <h1>Important blog</h1>
            {/* {data.map((recent) => {
                return (
                    <div key={recent.id}>
                        <h3>{recent.id}</h3>
                        <h5>{recent.title}</h5>
                    </div>
                )
            })} */}
        </div>
    )
}

export default Blogs
